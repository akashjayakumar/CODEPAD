# CODEPAD
Codepad is an AI-powered Python code comparison tool that detects changes, checks indentation, validates syntax, and provides smart insights using models like Google Gemini. It runs on a lightweight FastAPI backend with an optional React UI and can be deployed easily on platforms like Railway, Render, or Cloud Run
